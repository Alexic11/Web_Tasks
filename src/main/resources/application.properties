spring.application.name=Web_Tasks
server.port=9800
server.forward-headers-strategy=framework


spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/task_app?useUnicode=true&characterEncoding=utf8&serverTimezone=Europe/Sarajevo}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#spring.jpa.hibernate.ddl-auto=validate
#.jpa.open-in-view=false

# ako koristi? Flyway kasnije:
# spring.flyway.enabled=true


app.attachments.storage-path=uploads


# Global admin nalog za MVP varijantu bez posebne role u bazi.
app.security.global-admin-email=admin@local
